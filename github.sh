#!/usr/bin/bash

git config --global user.email "arifzynxd@gmail.com"
git config --global user.name "ArifzynXD"
git init
git config --global --add safe.directory /storage/emulated/0/api
git add .
git commit -m "new update"
git remote add origin https://github.com/Arifzxa19/my-api
